name: "Extended security codeql analysis"

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  call-reusable-codeql:
    uses: KL-Global/kidsloop-reusable-workflows/.github/workflows/reusable-extended-sec-codeql.yml@main
    with:
      language: 'javascript'
