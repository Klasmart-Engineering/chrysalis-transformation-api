name: "CodeQL analysis"

on:
  push:
    branches:  
      - master
  pull_request:

jobs:
  call-reusable-codeql:
    uses: KL-Engineering/kidsloop-reusable-workflows/.github/workflows/codeql-analysis.yml@main
    with:
      language: 'javascript'